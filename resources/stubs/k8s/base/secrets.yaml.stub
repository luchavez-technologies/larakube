apiVersion: v1
kind: Secret
metadata:
  name: laravel-secrets
type: Opaque
stringData:
  APP_KEY: {{APP_KEY}}
  DB_PASSWORD: {{DB_PASSWORD}}
  MEILISEARCH_KEY: {{MEILI_MASTER_KEY}}
  # Add other secrets here
