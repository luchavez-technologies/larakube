apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  template:
    spec:
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: {{APP_NAME}}-postgres-pvc
